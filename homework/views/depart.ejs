<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="./frontUser.css" />
  </head>
  <body id="departBg">
    <header><%- include('./header') %></header>
    <% if(name){ %>
    <h3><%= name %>님 환영합니다</h3>
    <% } %>

    <h3>새로운 항공기 출발 정보 등록</h3>
    <div>
      <form id="form" method="post">
        <label>airline:</label>
        <input type="text" name="airline" />
        <!-- "ariline"을 "airline"으로 수정 -->
        <br />
        <label>to:</label>
        <input type="text" name="to" />
        <br />
        <label>gate:</label>
        <input type="text" name="gate" />
        <button type="submit">제출</button>
      </form>
    </div>
    <div>
      <div id="editPopup">
        <div>
          <label for="editAirline">Airline :</label>
          <input id="editAirline" type="text" />
        </div>
        <div>
          <label for="editTo">To :</label>
          <input id="editTo" type="text" />
        </div>
        <div>
          <label for="editGate">Gate :</label>
          <input id="editGate" type="text" />
        </div>
        <button id="editPopupButton">수정하기</button>
      </div>
      <table>
        <thead>
          <tr>
            <th>airline</th>
            <th>to</th>
            <th>gate</th>
          </tr>
        </thead>
        <tbody id="departTbody">
          <% flights.forEach((item) => { %>
          <!-- map 함수 대신 forEach 함수로 변경 -->
          <tr>
            <td><%= item.airline %></td>
            <!-- th 대신 td로 수정 및 <%= %> 추가 -->
            <td><%= item.to %></td>
            <td><%= item.gate %></td>
            <th><button id="edit" data-id="<%= item.id %>">수정</button></th>
            <th><button id="delete" data-id="<%= item.id %>">삭제</button></th>
          </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
    <footer><%- include('./footer') %></footer>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="./frontUser.js"></script>
  </body>
</html>
